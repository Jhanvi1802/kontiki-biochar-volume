# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
kontiki_venv/

# OS
Thumbs.db
.DS_Store

# Auto-downloaded model weight — ultralytics fetches this on first run,
# so it doesn't need to live in the repo.
mobile_sam.pt

# Word temp/lock files
~$*.docx

# Dashboard runtime working directory (uploads/outputs)
video_volume/dashboard/_work/

# Large test point cloud (regenerate via the Colab notebook)
dense.ply

# stray PDFs dropped into the project root
/*.pdf
